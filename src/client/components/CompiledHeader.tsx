import React from 'react';
import PageHeader from './PageHeader';

const CompiledHeader = (): React.ReactElement => (
  <PageHeader title="Compiled Files" message="Files that have been compiled successfully." />
);

export default CompiledHeader;
