import React from 'react';
import PageHeader from './PageHeader';

const TemplatesHeader = (): React.ReactElement => (
  <PageHeader title="Templates" message="Templates that have been uploaded and validated successfully." />
);

export default TemplatesHeader;
